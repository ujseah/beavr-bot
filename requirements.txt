# Minimal dependencies to run unit/integration tests in CI without heavy/hardware stacks
# Keep in sync with imports used by test paths and modules under test

typing_extensions>=4.9.0
numpy
scipy>=1.15.2
pyzmq>=26.2.1
opencv-python-headless>=4.9.0

# Test tools
pytest>=8.1.0
pytest-timeout>=2.4.0
pytest-cov>=5.0.0
